# VIP Factory checkpoint — 2026-09-01 00:00 Europe/London

## Source of truth
- Main remains `dbca0f150ba85d52ccebe5c06e7b8f467ff0222b` at invocation start; no merge performed.
- Draft PR #1 remains the isolated recovery lane on `factory/source-recovery`.
- MVP Sprint Mode throughput escalation applied: the known reversible recovery path was converted into implementation rather than another analysis-only checkpoint.

## Implementation progress
First maintainable source/test vertical increment now exists on the recovery branch:
- `src/domain/pay.js`: framework-independent scheduled-shift forecast using integer pence at the domain boundary.
- `test/pay.test.js`: six deterministic tests covering ordinary shift forecast, no invented premium, explicit premium, overnight dated shift, spring-forward elapsed time, and impossible break rejection.
- `package.json`: zero-dependency Node test command (`node --test`).

The domain deliberately does not infer Sunday/night/weekend premiums. Employer premiums must be supplied explicitly. Historical Loren prototype constants are not generic defaults.

## Agentic loop
- Value gate: PASS. Exact-money scheduled-shift forecast is the first critical vertical-slice domain capability and unblocks Worked/persistence/UI slices.
- Executor: implementation commits `93aba56`, `afe19de`, `48f92a2`.
- Deterministic evidence: tests have been authored but no connected CI execution result is yet observable, so they are NOT reported as passing. This is an explicit remaining release gate.
- Sceptical review: serial logical review only in this observable execution context. Review focus found no new employer-rule inference; DST fixture uses explicit offset-aware instants. Remaining concern: premium segment API currently trusts caller segmentation and does not prove segments fit inside worked minutes; this should be tightened before release.
- Repair cycles: 0 completed in this invocation; one review concern queued.
- Release: no merge. PR remains draft until test execution, persistence/recovery, UI/parity and adversarial gates are satisfied.

## Code metrics
Unmerged first-party implementation added this invocation: production `src/domain/pay.js` 30 nonblank/source lines approximately; test `test/pay.test.js` 67 source lines approximately. Package/config excluded. Exact merged production/test delta: 0 / 0 because PR #1 remains unmerged. These approximate branch counts must not be presented as published LOC.

## Ordered next work
1. Tighten premium-segment validation so premium minutes cannot exceed worked minutes / double-count without explicit model semantics.
2. Establish observable CI/test execution for the zero-dependency harness.
3. Add Scheduled -> Worked immutable history model and tests as the next vertical increment.
4. Add versioned local persistence/reload around that model.
5. Wire minimal mobile UI only after domain/history contracts are executable.

## Capacity / execution truth
- Observable execution contexts: 1. No independent sub-agent execution claimed.
- Useful implementation work was possible and started immediately; no platform capacity warning observed.
- Checkpoint usefulness: rescued prior analysis-heavy pattern by producing maintainable source + tests.
- Exact continuation reason: meaningful safe implementation remains; no Shaun decision is required.
