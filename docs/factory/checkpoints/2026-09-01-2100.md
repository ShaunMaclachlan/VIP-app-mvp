# VIP Factory checkpoint — 2026-09-01 21:00 Europe/London

## Source of truth
- Main remains `dbca0f150ba85d52ccebe5c06e7b8f467ff0222b`; no merge performed.
- Draft PR #1 remains isolated on `factory/source-recovery`.
- Starting PR head was `9cfb404893f1d3d2111b0bdc35a75902810e424b`; ending implementation head before this checkpoint is `a969a78ac239fdb35619c2b12663364b3df7819e`.
- PR #1 is 28 commits ahead of main, with 22 changed files and 988 additions overall (including docs/config).

## Builder queue execution
The new NEW/REWORK builder loop was exercised rather than waiting on failed CI.

### REWORK completed
- CI run `33535204567` exposed 4 failures out of 26 tests. All four shared one contract defect: `forecastEffectiveShift()` returned `grossPence`, while application/reconciliation consumers correctly expected the public forecast field `totalPence`.
- Repaired `src/domain/forecast.js` by preserving `grossPence` detail while adding `totalPence: pay.grossPence` to the forecast contract.
- Repair commit: `80c021f43e99d8f9cac2492160f045b713e03134`.

### NEW slice completed
Without waiting idle for the repair CI, the builder continued into the next approved slice: persisted Planned -> Worked -> Paid reconciliation.
- `src/app/shiftWorkflow.js` now exposes `recordActualPaid()` and `getReconciliation()` and persists Paid truth alongside schedule/Worked history.
- `test/shiftWorkflow.test.js` adds an end-to-end persistence/reload case proving Planned, Worked, Paid and the pay variance survive reload.
- Implementation commits: `dfdb01f559ae84f3150113d34fd2face7f1124e5`, `a969a78ac239fdb35619c2b12663364b3df7819e`.
- CI run `33552625603` for latest head is in progress at checkpoint time; do not claim latest tests passing until it completes.

## Deterministic quality evidence
- Failed CI was diagnosed from job logs rather than guessed: 22/26 passed, failures were reconciliation/workflow assertions where `totalPence` was `undefined` or variance became `NaN`.
- The repair is deliberately a compatibility contract fix, not a golden-result alteration: the pay engine continues to calculate `grossPence` in integer pence and the forecast layer aliases that existing result as the application-facing `totalPence`.
- Unknown employer break rules remain explicit `needs-pay-rule`; no employer-specific default introduced.
- Serial logical sceptical review only; one observable execution context, no independent sub-agent claimed.

## Code metrics
Against main, first-party unmerged production files under `src/` total 205 LOC and tests under `test/` total 329 LOC, from GitHub compare per-file additions. Docs/config/workflow/package files excluded.
- Published/merged since prior human-facing checkpoint: production +0/-0; tests +0/-0.
- Current merged maintainable source/test LOC on main: 0/0 for the recovered source tree; existing compiled deployment assets excluded.
- Current unmerged recovery implementation: 205 production LOC / 329 test LOC; test:prod 160.5%.
- Since the 15:00-era failing head, this run added a forecast-contract repair plus the Paid workflow integration and one end-to-end workflow test. Exact per-window added/deleted LOC cannot be reproduced from the available prior human-facing SHA with confidence, so it is not guessed.

## Builder status and queue
- Builder status: ACTIVE; no hard coding blocker.
- REWORK queue: latest-head CI result pending. If red, repair immediately; if green, no known actionable CI rework.
- NEW queue: (1) minimal mobile UI shell for add scheduled -> forecast -> confirm Worked -> changed forecast -> reload; (2) visible unknown-pay-rule/persistence error states; (3) Paid/reconciliation presentation in UI.
- This invocation: REWORK cycles completed 1; NEW slices started 1 / completed 1.

## Release / continuation
- No merge: PR remains draft and latest CI is still in progress; mobile UI/parity/recovery gates are not yet satisfied.
- Next highest-value action is observe/repair latest CI, then build the minimal mobile UI rather than another documentation pass.
- Checkpoint usefulness: rescued a red implementation from CI and, while CI cycled, created the next approved functional slice instead of idling.
- Observable platform restriction: none observed.
- No product-owner decision required.
